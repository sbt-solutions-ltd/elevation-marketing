{
  "name": "elevation-marketing",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "generate:gh": "cross-env DEPLOY_ENV=gh-pages nuxt generate",
    "deploy": "npm run generate:gh && gh-pages --dotfiles -d .output/public",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "autoprefixer": "^10.4.16",
    "gh-pages": "^6.3.0",
    "nuxt": "^3.9.0",
    "nuxt-icon": "^0.6.8",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0",
    "vue": "^3.4.3",
    "vue-router": "^4.2.5"
  }
}
